{
  "name": "Painel de Manobras (IA) — Minha Anamnese",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "formTitle": "Manobras de exame físico",
        "formDescription": "Escolha a ação e envie. 'Rodar automação' gera/corrige as linhas marcadas no Notion; 'Publicar no site' sincroniza o que já foi revisado.",
        "formFields": {
          "values": [
            {
              "fieldLabel": "acao",
              "fieldType": "dropdown",
              "fieldOptions": {
                "values": [
                  { "option": "Rodar automacao (gerar/corrigir)" },
                  { "option": "Simular (dry-run)" },
                  { "option": "Publicar no site (sync)" },
                  { "option": "Enfileirar incompletas" },
                  { "option": "Preview de geracao" }
                ]
              },
              "requiredField": true
            },
            {
              "fieldLabel": "limite",
              "placeholder": "Quantas linhas processar (padrao 5)",
              "requiredField": false
            },
            {
              "fieldLabel": "nome_da_manobra",
              "placeholder": "So para 'Preview de geracao' (ex.: Sinal de Blumberg)",
              "requiredField": false
            }
          ]
        },
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "a1b2c3d4-e5f6-4708-9a1b-2c3d4e5f6071",
      "name": "Formulário - Manobras",
      "type": "n8n-nodes-base.formTrigger",
      "typeVersion": 2.1,
      "position": [0, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://minhaanamnese.onrender.com/api/admin/{{ ({ 'Rodar automacao (gerar/corrigir)': 'maneuvers/automation-run', 'Simular (dry-run)': 'maneuvers/automation-run', 'Publicar no site (sync)': 'physical-exam-maneuvers/sync', 'Enfileirar incompletas': 'maneuvers/queue-incomplete', 'Preview de geracao': 'maneuvers/generate-preview' })[$json.acao] }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.acao === 'Publicar no site (sync)' ? 'SEU_ADMIN_SYNC_SECRET' : 'SEU_PROTOCOL_AUTOMATION_SECRET' }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ limit: Number($json.limite) || 5, dryRun: $json.acao === 'Simular (dry-run)', name: $json.nome_da_manobra || '' }) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 300000
        }
      },
      "id": "b2c3d4e5-f607-4819-a2b3-4c5d6e7f8192",
      "name": "Chamar API admin",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [240, 0]
    },
    {
      "parameters": {
        "jsCode": "// Resumo legível no celular: a resposta crua da API é grande demais para\n// ler na tela do formulário.\nconst r = $json || {};\nconst d = r.data || {};\nconst linhas = [];\n\nif (r.success === false) {\n  linhas.push('ERRO: ' + (r.error || 'falha desconhecida'));\n  if (r.details) linhas.push(String(r.details).slice(0, 300));\n} else if (d.maneuver) {\n  const m = d.maneuver;\n  linhas.push('Preview gerado (NAO escreveu no Notion)');\n  linhas.push('Manobra: ' + (m.name || '-'));\n  linhas.push('Regiao: ' + (m.category || '-'));\n  linhas.push('Trava: ' + m.automation_status + ' / ' + m.review_status);\n  linhas.push('');\n  linhas.push('Quando fazer: ' + (m.when_to_perform || '(vazio)'));\n  linhas.push('');\n  linhas.push('Como executar: ' + (m.how_to_perform || '(vazio)'));\n  linhas.push('');\n  linhas.push('Achado positivo: ' + (m.positive_finding || '(vazio)'));\n} else if (Array.isArray(d.results)) {\n  linhas.push(d.dryRun ? 'SIMULACAO (nada foi escrito)' : 'Automacao executada');\n  linhas.push('Encontradas: ' + d.found + ' | geradas: ' + d.generated + ' | corrigidas: ' + d.corrected + ' | erros: ' + d.errors);\n  for (const x of d.results) {\n    linhas.push((x.ok ? 'OK' : 'ERRO') + ' - ' + x.action + ': ' + (x.titulo || '-') + ' -> ' + (x.newStatusAutomacao || x.error || ''));\n  }\n  if (d.processed === 0) linhas.push('(nenhuma linha marcada \"a gerar\" ou \"a corrigir\")');\n} else if (Array.isArray(d.items)) {\n  linhas.push('Fila de manobras incompletas');\n  linhas.push('Publicadas: ' + d.totalPublished + ' | incompletas: ' + d.totalIncomplete + ' | enfileiradas: ' + d.queued);\n  for (const x of d.items) {\n    linhas.push('- ' + (x.name || '-') + ' (faltando: ' + (x.missingFields || []).join(', ') + ')');\n  }\n  if (d.totalIncomplete === 0) linhas.push('Nenhuma manobra publicada com campo vazio.');\n} else if (d.totalFromNotion !== undefined) {\n  linhas.push('Sync Notion -> site');\n  linhas.push('Sincronizadas: ' + d.persisted + '/' + d.totalFromNotion + ' | publicadas: ' + d.publishedAvailable);\n  const retidas = d.held || [];\n  if (retidas.length) {\n    linhas.push('Retidas (aguardando revisao humana):');\n    for (const x of retidas) linhas.push('- ' + (x.name || '-') + ' (' + x.automationStatus + ')');\n  }\n  const puladas = d.skipped || [];\n  for (const x of puladas) {\n    linhas.push('PULADA: ' + (x.name || '-') + ' -> ' + (x.reasons || []).join(', '));\n  }\n} else {\n  linhas.push(JSON.stringify(r).slice(0, 500));\n}\n\nreturn [{ json: { resultado: linhas.join('\\n') } }];"
      },
      "id": "c3d4e5f6-0718-492a-b3c4-5d6e7f8192a3",
      "name": "Resumo",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 0]
    }
  ],
  "connections": {
    "Formulário - Manobras": {
      "main": [
        [
          {
            "node": "Chamar API admin",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Chamar API admin": {
      "main": [
        [
          {
            "node": "Resumo",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {},
  "meta": {}
}
